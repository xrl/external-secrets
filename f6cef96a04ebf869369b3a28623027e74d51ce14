Fork-only native amd64/arm64 image builds pin Go 1.26.5 and retain all providers. Read-only jobs build checked archives; only exact xrl integration push/dispatch jobs receive package writes. Child digest records drive the unique run/attempt manifest. Legacy publishers are upstream-only; integration PRs leave legacy CI. Preparation and read-only/network-denied verification hooks currently perform help smoke tests only: SDK cache API and CPU compatibility validation remain pending. Re-run all jobs to avoid mixing attempt-scoped artifacts. No provider/dependency/WASM changes.
