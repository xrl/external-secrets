Add early fallible Feature.Prepare without changing existing Initialize. Controller owner is process-lifetime: manager timeout is not proof all reconcilers ended. CLI alone closes its owner and bypasses Kubernetes/auth. Cache is prepared by exact all-provider binary and checked read-only in fresh process; retain CPU/LSE keys. This checkpoint depends on reviewed SDK API; immutable published module pins and validation follow separately.
